{
  "semantics": "inventory",
  "validatedAt": null,
  "caveat": "ARG returns configuration inventory only, not utilization, latency, capacity sufficiency or a compliance verdict. Missing properties, limited permissions, truncation and zero rows do not establish a pass. Compare with workload telemetry and requirements; this query has not been executed or validated against Azure.",
  "queries": [
    {
      "id": "aks-pools",
      "arg": "Resources\n| where type =~ 'microsoft.containerservice/managedclusters'\n| mv-expand pool = properties.agentPoolProfiles\n| project id, name, resourceGroup, subscriptionId, poolName = tostring(pool.name),\n    vmSize = tostring(pool.vmSize), enableAutoScaling = pool.enableAutoScaling,\n    minCount = pool.minCount, maxCount = pool.maxCount, osDiskType = tostring(pool.osDiskType),\n    kubeletConfig = pool.kubeletConfig, linuxOSConfig = pool.linuxOSConfig\n",
      "sourceRefs": [
        "aks-custom",
        "aks-autoscale",
        "aks-storage",
        "arg-language"
      ],
      "assignments": [
        "831c2872-c693-4b39-a887-a561bada49bc",
        "90ce65de-8e13-4f9c-abd4-69266abca264",
        "24367b33-6971-45b1-952b-eee0b9b588de"
      ]
    },
    {
      "id": "gateway-skus",
      "arg": "Resources\n| where type =~ 'microsoft.network/virtualnetworkgateways'\n| project id, name, resourceGroup, subscriptionId, location,\n    gatewayType = tostring(properties.gatewayType), sku = properties.sku\n",
      "sourceRefs": [
        "er",
        "arg-language",
        "arg-types"
      ],
      "assignments": [
        "d4cd21b0-8813-47f5-b6c4-cfd3e504547c"
      ]
    },
    {
      "id": "vnet-address-plan",
      "arg": "Resources\n| where type =~ 'microsoft.network/virtualnetworks'\n| project id, name, resourceGroup, subscriptionId, location,\n    addressSpace = properties.addressSpace, subnets = properties.subnets\n",
      "sourceRefs": [
        "vnet",
        "arg-language",
        "arg-types"
      ],
      "assignments": [
        "33aad5e8-c68e-41d7-9667-313b4f5664b5"
      ]
    }
  ],
  "removed": [
    {
      "id": "0b5a380c-4bfb-47bc-b1d7-dcfef363a61b",
      "arg": "cdnresources | where type =~ 'microsoft.cdn/profiles/origingroups/origins' | extend frontDoorId = substring(id, 0, indexof(id, '/origingroups')) | extend originGroupId = substring(id, 0, indexof(id, '/origins')) | join kind=inner (cdnresources | where type =~ 'microsoft.cdn/profiles/origingroups' | extend originGroupName = name | extend hasHealthProbe = isnotnull(properties.healthProbeSettings)) on $left.originGroupId == $right.id | summarize numberOrigins = count() by originGroupId, subscriptionId, frontDoorId, hasHealthProbe, originGroupName | extend compliant = not(numberOrigins == 1 and hasHealthProbe) | project id = frontDoorId, compliant",
      "reason": "Remove undocumented CdnResources query and origin-count compliance verdict."
    },
    {
      "id": "a13f72f3-8f5c-4864-95e5-75bf37fbbeb1",
      "arg": "cdnresources | where type =~ 'microsoft.cdn/profiles/origingroups' | extend frontDoorId = substring(id, 0, indexof(id, '/origingroups/')) | extend compliant = (isnull(properties['healthProbeSettings']['probeRequestType']) or toupper(properties['healthProbeSettings']['probeRequestType']) == 'HEAD') | project compliant, id=frontDoorId",
      "reason": "Remove undocumented ARG query and null-as-pass logic; validate origin compatibility."
    }
  ]
}
